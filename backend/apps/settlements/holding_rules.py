# Правила холдинга по взаиморасчётам (ХОЛ-30…33).
#
# ВСЕ значения — ПРЕДВАРИТЕЛЬНЫЕ ДОПУЩЕНИЯ.
# TODO: заменить на реальные ХОЛ-30…33, когда появятся правила холдинга.
#
# Сервисы читают правила ТОЛЬКО отсюда: поменять правило = поменять конфиг.
from decimal import Decimal

HOLDING_RULES = {
    # ХОЛ-30: при одобренной передаче автоматически фиксируется долг
    # debtor(получатель) → creditor(отправитель) на сумму передачи.
    "auto_debt_on_transfer": True,
    # ХОЛ-31: взаимозачёт встречных долгов между двумя бизнесами
    # до меньшей суммы. "bilateral" | "off".
    "netting_mode": "bilateral",
    # ХОЛ-32: передачи на сумму СВЕРХ порога требуют одобрения владельцем
    # (а не только бухгалтером). Decimal("0") = порога нет.
    "owner_approval_threshold": Decimal("50000"),
    # ХОЛ-33: бартер может закрывать встречный долг на сумму оценки
    # (под контролем бухгалтера).
    "barter_can_close_debt": True,
    # ХОЛ-33: открытый долг помечается «просрочен» после N дней. 0 = не помечать.
    "debt_overdue_days": 30,
}
